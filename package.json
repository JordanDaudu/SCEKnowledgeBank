{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "preinstall": "sh -c 'rm -f package-lock.json yarn.lock; case \"$npm_config_user_agent\" in pnpm/*) ;; *) echo \"Use pnpm instead\" >&2; exit 1 ;; esac'",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck",
    "regression": "pnpm run typecheck && pnpm -r --if-present run test && pnpm --filter @workspace/api-server run seed:demo && PLAYWRIGHT_BASE_URL=https://$REPLIT_DEV_DOMAIN pnpm --filter @workspace/web exec playwright test --grep \"sprint 2 smoke\"",
    "regression:local": "STORAGE_DRIVER=local pnpm run regression",
    "regression:gcs": "STORAGE_DRIVER=gcs pnpm run regression"
  },
  "private": true,
  "devDependencies": {
    "typescript": "~5.9.2",
    "prettier": "^3.8.1"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "sharp"
    ],
    "overrides": {
      "brace-expansion@>=2.0.0 <2.0.3": "^2.0.3",
      "fast-uri@<3.1.2": "^3.1.2",
      "lodash@<4.18.0": "^4.18.0",
      "path-to-regexp@>=8.0.0 <8.4.0": "^8.4.0",
      "picomatch@>=2.0.0 <2.3.2": "^2.3.2",
      "picomatch@>=4.0.0 <4.0.4": "^4.0.4",
      "postcss@<8.5.10": "^8.5.10",
      "qs@>=6.0.0 <6.15.2": "^6.15.2",
      "uuid@<11.1.1": "^11.1.1",
      "yaml@>=2.0.0 <2.8.3": "^2.8.3"
    }
  }
}
