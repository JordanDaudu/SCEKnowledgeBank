You are continuing the existing Knowledge Bank / בנק ידע project.

Important:
Do NOT restart the project.
Do NOT rewrite the architecture.
Do NOT remove existing working features.
Continue from the current codebase and finish the remaining Sprint 2 acceptance gaps.

Current status:
A previous hardening pass already fixed several important issues:
- Added artifacts/web/src/lib/api-url.ts
- Preview/download signed URLs now resolve through VITE_API_BASE
- Upload XHR now resolves through apiUrl()
- Added documentFiles.displayFilename while preserving documentFiles.originalFilename
- Updated OpenAPI/client/Zod types for displayFilename
- Frontend/backend upload file types were aligned
- Requests fulfillment RBAC now allows lecturer/admin
- Tests were added for request fulfillment

Your task now is to finish the remaining Sprint 2 gaps and fix a few discovered correctness issues.

Before coding:
1. Read:
   /.local/tasks/knowledge-bank-sprint-2-mvp.md
2. Inspect:
   artifacts/web/src/pages/browse.tsx
   artifacts/web/src/pages/document-detail.tsx
   artifacts/web/src/pages/upload.tsx
   artifacts/api-server/src/routes/documents.ts
   artifacts/api-server/src/lib/env.ts
   artifacts/api-server/src/index.ts
   artifacts/api-server/src/repositories/requests.repo.ts
   artifacts/api-server/src/services/requests.service.ts
   lib/api-spec/openapi.yaml
   lib/api-zod/src/generated/api.ts
   lib/db/drizzle.config.ts
   artifacts/api-server/src/scripts/seed.ts
   .env.example
   replit.md
   README.md

Required fixes:

1. Fix backend query parsing for document filters.
   Problem:
   OpenAPI/Zod currently generates dateFrom/dateTo as zod.date(), but Express query params arrive as strings.
   This means /documents?dateFrom=2025-01-01 may fail validation.

   Required:
   - Make dateFrom/dateTo parse correctly from query strings.
   - Use zod.coerce.date() or route-level preprocessing.
   - Also verify tagIds works for both one selected tag and multiple selected tags.
   - Add tests for:
     - dateFrom as "2025-01-01"
     - dateTo as "2025-01-31"
     - single tagIds query value
     - repeated tagIds query values
   - Regenerate API/Zod clients if OpenAPI changes.

2. Complete Browse page Sprint 2 UI requirements.
   Current browse.tsx is card-only and too large.
   Required:
   - Add card/table toggle.
   - Extract DocumentCards component.
   - Extract DocumentTable component.
   - Add search suggestions while typing using existing /documents/suggestions endpoint.
   - Add RecentlyViewedStrip using /documents/recent.
   - Add clear loading, empty, no-results, and error states.
   - Keep URL-synced query state.
   - Keep debounce behavior.
   - Keep filters: course, lecturer, semester, academic year, material type, category, tags, upload date range.
   - Keep sorting: newest, oldest, title, popularity.
   - Keep pagination.

3. Split oversized frontend pages.
   Refactor without changing behavior:
   - browse filters
   - browse cards
   - browse table
   - recently viewed strip
   - document preview panel
   - metadata panel
   - edit metadata modal
   - comments thread
   - upload dropzone
   - upload queue
   - upload metadata form

   Target:
   - No page file should remain huge.
   - Keep components focused.
   - Avoid circular imports.
   - Keep generated API hooks/types where available.

4. Add real Drizzle migration files.
   Current setup relies mainly on drizzle-kit push.
   Sprint 2 requires generated migrations.
   Required:
   - Generate migration SQL files for the current schema.
   - Include pg_trgm extension support in migration or clearly handle it in DB init.
   - Update package scripts if needed:
     - db:generate
     - db:migrate
   - Update Docker/api-init to use the intended migration path if practical.
   - Do not break docker compose demo.

5. Add seed fixture files.
   Sprint 2 requested fixtures under:
   lib/db/src/seed/fixtures/

   Required:
   - Add real/simple fixture assets:
     - at least 2 PDFs
     - at least 1 PNG or JPG image
     - at least 1 TXT or Markdown file
   - Update seed script to load several documents from these fixtures.
   - Keep current rich seed data volume:
     - 3 demo users
     - 6+ courses
     - 5+ categories
     - 10+ tags
     - 30+ documents
     - comments
     - requests
     - recent-view rows

6. Replace manual env parsing with Zod-based env validation.
   Required:
   - Make artifacts/api-server/src/lib/env.ts use Zod.
   - Make env.ts the only source of truth for env parsing.
   - Update artifacts/api-server/src/index.ts to use env.port instead of reading process.env.PORT directly.
   - Keep safe development defaults for non-secret dev values.
   - Keep production secret requirements for SESSION_SECRET and SIGNED_URL_SECRET.
   - Validate:
     DATABASE_URL
     PORT
     NODE_ENV
     WEB_ORIGIN
     SESSION_SECRET
     SIGNED_URL_SECRET
     SIGNED_URL_TTL_SECONDS
     MAX_UPLOAD_MB
     ALLOWED_MIME_TYPES
     STORAGE_DRIVER
     STORAGE_LOCAL_ROOT

7. Update .env.example.
   Add missing frontend build variables:
   - VITE_API_BASE=http://localhost:8080
   - VITE_MAX_UPLOAD_MB=50

   Make sure README and replit.md also document:
   - demo users
   - upload size limit
   - allowed MIME types
   - signed preview/download behavior
   - local run commands
   - Docker run commands
   - migration commands
   - seed commands

8. Remove raw API endpoint strings from page components.
   Current upload.tsx still contains:
   apiUrl("/api/documents/upload")

   Required:
   - Move the upload URL string to a shared API URL helper or generated-client-adjacent helper.
   - Keep XMLHttpRequest upload progress support.
   - Do not hardcode /api/... route strings inside page components.

9. Make request voting race-safe.
   Current logic pre-checks and then inserts.
   Required:
   - Use DB-level onConflictDoNothing or catch unique violation 23505.
   - Return a clean 409 conflict or a consistent idempotent success.
   - Choose one semantic and document it.
   - Add tests for duplicate vote and insert-conflict race behavior.

10. Centralize material type values.
   Problem:
   Seed data includes material types like review-notes and cheat-sheet, but frontend dropdowns do not list them.
   Required:
   - Create one shared frontend constant or backend-derived list for material types.
   - Include all material types used by seed data:
     lecture-notes
     problem-set
     exam
     syllabus
     slides
     project-report
     textbook
     review-notes
     cheat-sheet
   - Use it in browse, upload, and edit metadata modal.

11. Verify signed preview/download still work.
   Required:
   - Direct /documents/:id/preview without token must return 401.
   - Invalid/expired token must return 401.
   - Valid preview token should stream PDF/image inline.
   - Valid download token should stream attachment.
   - Frontend should resolve all signed URLs through the API base URL.

12. Final checks.
   Run and fix all failures:
   - pnpm --filter @workspace/api-spec run codegen
   - pnpm run typecheck
   - pnpm run build
   - pnpm --filter @workspace/api-server run test
   - docker compose up --build

Acceptance checklist:
- student@demo / lecturer@demo / admin@demo can log in with demo1234.
- Lecturer/admin can upload multiple valid files.
- Unsupported file types show immediate client validation and server validation.
- Oversized files show clear client/server errors.
- Duplicate filenames preserve originalFilename and use displayFilename with suffix.
- Browse supports search, suggestions, filters, sorting, pagination, card/table toggle, recently viewed, loading, empty, no-results, and error states.
- Date filters work from the browser.
- Single and multiple tag filters work from the browser.
- Document detail supports PDF/image preview, download, comments, nested replies, page pinning, and edit metadata.
- Direct preview/download without token is rejected.
- Invalid/expired token is rejected.
- Requests board supports create, vote once, status display, and lecturer/admin fulfillment.
- Request voting is race-safe.
- RBAC is enforced server-side.
- Audit logs are written for upload/update/delete/comment/request/vote.
- Real migration files exist.
- Seed fixtures exist under lib/db/src/seed/fixtures/.
- Zod env validation is in place.
- Typecheck passes.
- Build passes.
- API tests pass.
- Docker demo works from a clean start.