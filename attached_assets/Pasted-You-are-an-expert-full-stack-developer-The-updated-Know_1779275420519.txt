You are an expert full-stack developer. The updated Knowledge Bank project is very close to Sprint 2 completion, but the final audit found several remaining blockers and demo-readiness issues. Implement the following fixes now using the existing React/Node/Express/Prisma/PostgreSQL stack. Do not introduce Python, FastAPI, Drizzle, or a second ORM.

Before coding:
- Inspect the existing Prisma schema.
- Inspect the document routes, document service, permissions service, frontend document detail page, upload page, comments service, request board service, demo seed script, and OpenAPI/client generation flow.
- Reuse the existing architecture and naming conventions.

Required fixes:

1. Harden course-aware permissions when updating documents

Problem:
The backend currently checks whether the user can edit the existing document, but if the PATCH body changes `courseId`, it may allow a lecturer to move a document into a course they do not teach.

Fix:
- In the document update service, if `courseId` is provided in the update body, validate the target course before applying the update.
- Admin may move documents to any course.
- Lecturer may only move documents to courses they teach or have lecturer course membership for.
- Student must not be able to move documents.
- If the target course does not exist, return a clean 400 or 404 error.
- If visibility is `RESTRICTED`, require a valid `courseId`.
- Do not allow a lecturer to convert a course-scoped restricted document into a restricted course-less document.
- Add backend tests:
  - lecturer can update metadata for a document in their course
  - lecturer cannot move a document into a course they do not teach
  - admin can move a document into any course
  - restricted document cannot be saved without a course
  - unauthorized user receives 403

Likely files:
- artifacts/api-server/src/services/documents.service.ts
- artifacts/api-server/src/services/permissions.service.ts
- artifacts/api-server/src/routes/documents.ts
- relevant backend test files

2. Return server-side permission flags in document DTOs

Problem:
The frontend guesses edit permissions using admin/uploader logic. Sprint 2 requires granular course-aware permissions.

Fix:
- Add permission flags to the backend document response DTO:
  - canView
  - canEdit
  - canDelete
  - canDownload
  - canComment
- These flags must be calculated on the server using the existing permissions service and authenticated user context.
- Include these flags in:
  - list documents
  - get document by ID
  - recently viewed documents
  - search results if applicable
- Update OpenAPI schema.
- Regenerate/update the React API client and Zod schemas according to the project’s existing generation flow.
- Update frontend document detail, cards, table, and action buttons to use server-provided permission flags instead of guessing.
- Ensure course lecturers can see edit/manage controls for documents in their courses even when they are not the uploader, if backend permissions allow it.

Likely files:
- artifacts/api-server/src/services/documents.service.ts
- artifacts/api-server/src/routes/documents.ts
- lib/api-spec/openapi.yaml
- lib/api-client-react/*
- lib/api-zod/*
- artifacts/web/src/pages/document-detail.tsx
- artifacts/web/src/components/browse/DocumentCards.tsx
- artifacts/web/src/components/browse/DocumentTable.tsx
- artifacts/web/src/components/document/*

3. Fix duplicate upload route link

Problem:
The duplicate upload result links to `/document/:id`, but the actual route is `/documents/:id`.

Fix:
- Update the upload UI so duplicate-file results link to `/documents/${duplicateOfDocumentId}`.
- Add a small frontend test or verify manually.

Likely file:
- artifacts/web/src/pages/upload.tsx

4. Make demo seed data prove metadata extraction and full-text file search

Problem:
The upload flow extracts metadata, but seeded demo documents do not fully include extracted metadata. This weakens the Sprint 2 demo.

Fix:
- Update the demo seed script so seeded document files are processed through the existing metadata extraction service, or backfilled with realistic metadata.
- Seed at least:
  - PDF page count
  - PDF/text extractedText
  - detectedTitle where available
  - image width/height for image fixture
  - thumbnailPath/thumbnailMimeType where thumbnail generation is supported
- On repeated seed runs, backfill metadata for existing seeded document files if those fields are null.
- Ensure at least several seeded documents can be found by searching terms that exist only in extracted text.
- Extend the demo verification script to confirm:
  - seeded documents exist
  - at least one PDF has pageCount
  - at least one document has extractedText
  - at least one image has image dimensions or thumbnail metadata
  - full-text search can find a seeded document by extracted text

Likely files:
- artifacts/api-server/src/scripts/seed-demo.ts
- artifacts/api-server/src/scripts/seed-demo.verify.ts
- artifacts/api-server/src/services/metadata.service.ts
- seed fixture files if needed

5. Clarify or improve Office-file metadata extraction

Problem:
The app accepts Office-style files, but metadata extraction mainly covers PDFs, text files, CSV/Markdown, and images.

Fix one of these two acceptable options:

Option A:
- Add Node-only extraction for DOCX/PPTX/XLSX where practical.
- Extract at least basic text and metadata.
- Do not use Python.

Option B:
- Keep Office files supported for upload/download/preview fallback.
- Add explicit fallback metadata behavior.
- Make sure UI does not imply deep extraction for Office files if not implemented.
- Ensure Office files get a correct fallbackIconType.

6. Re-parse @mentions when comments are edited

Problem:
Mentions are stored on comment creation, but edited comments should update mention storage too.

Fix:
- In the comment update service, when the body changes:
  - delete old mention rows for that comment
  - parse the new body
  - validate mentioned users
  - insert the new mention rows
- Return the updated mentions in the response DTO.
- Add backend tests.

Likely files:
- artifacts/api-server/src/services/comments.service.ts
- artifacts/api-server/src/repositories/comments.repo.ts
- comments tests

7. Decide and enforce request board course visibility

Problem:
Request board functionality works, but course-specific requests may be visible to users outside that course.

Fix:
- If the intended product behavior is global request board, document this clearly in code comments and demo notes.
- If course-aware access should apply, filter course-specific requests so:
  - Admin sees all
  - Course lecturer sees requests for their courses
  - Enrolled students see requests for their courses
  - Public/global requests remain visible if courseId is null
- Apply the same rule to request detail/voting if applicable.
- Add tests.

Likely files:
- artifacts/api-server/src/services/requests.service.ts
- artifacts/api-server/src/repositories/requests.repo.ts
- artifacts/web/src/pages/requests.tsx

8. Remove stale Drizzle references from documentation/comments

Problem:
Runtime code is now Prisma-based, but some docs/comments still mention Drizzle.

Fix:
- Remove or update stale Drizzle references from README, replit docs, Dockerfile comments, and setup instructions.
- Make sure all setup instructions mention Prisma migrations/seeding.

Likely files:
- README.md
- replit.md
- artifacts/api-server/Dockerfile
- package scripts documentation

9. Final verification

After implementation, run:

pnpm --filter @workspace/db run generate
pnpm run typecheck
pnpm --filter @workspace/api-server test
pnpm --filter @workspace/api-server seed:demo
pnpm --filter @workspace/api-server seed:demo:verify

Then manually verify these demo flows:
1. Login as admin.
2. Approve pending lecturer.
3. Login as lecturer and manage materials for owned courses.
4. Verify lecturer cannot move a document into an unrelated course.
5. Login as student and browse/search/filter documents.
6. Search for text that exists inside a seeded file’s extractedText.
7. Test duplicate upload and confirm the “View original” link works.
8. Test storage quota display.
9. Test nested comments, page-linked comments, and mentions.
10. Test request board voting.
11. Test signed preview/download URLs.

Acceptance criteria:
- No Python/FastAPI added.
- No Drizzle runtime code.
- Prisma remains the only ORM.
- Typecheck passes.
- Backend tests pass.
- Demo seed and verification pass.
- Sprint 2 demo can show upload queue, validation, quota, duplicate detection, metadata extraction, full-text search, filters, sorting, pagination, recent history, nested comments, page comments, mentions, request board voting, RBAC, course-aware permissions, and signed timed preview/download URLs.