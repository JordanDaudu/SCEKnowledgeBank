You are an expert full-stack developer. The Knowledge Bank Sprint 2 implementation is mostly complete. Do not rewrite major systems. Do not reintroduce Drizzle, Python, or FastAPI. Keep the existing React / Node / Express / Prisma / PostgreSQL architecture.

Implement only these final demo-readiness fixes:

1. Align demo seed commands

Problem:
The rich Sprint 2 demo data exists in `seed:demo`, but some startup/Docker/docs flows may still run the old/basic `seed` command.

Fix:
- Make sure the normal demo/startup path runs the rich demo seed.
- Either:
  - change the default `seed` script to call `seed:demo`, or
  - update Docker/entrypoint/Replit startup docs to run `seed:demo`.
- Make sure the demo login buttons work immediately after seeding.

Verify these demo users exist after seeding:
- admin@knowledgebank.demo
- maya.cohen@knowledgebank.demo
- noa.student@knowledgebank.demo
- restricted.student@knowledgebank.demo
- pending.lecturer@knowledgebank.demo
- disabled.user@knowledgebank.demo

2. Update documentation and demo credentials

Update README, Replit notes, setup docs, and any visible demo instructions.

Replace old credentials such as:
- student@demo
- lecturer@demo
- admin@demo

with:

Password for all demo users:
Demo1234!

Admin:
admin@knowledgebank.demo

Lecturers:
maya.cohen@knowledgebank.demo
daniel.levi@knowledgebank.demo

Pending Lecturer:
pending.lecturer@knowledgebank.demo

Students:
noa.student@knowledgebank.demo
amir.student@knowledgebank.demo
yael.student@knowledgebank.demo
restricted.student@knowledgebank.demo

Disabled:
disabled.user@knowledgebank.demo

Also update documentation so it reflects the current Prisma-based architecture, not Drizzle.

3. Add request-creation course validation

The request board is already course-aware for visibility/voting. Add the same protection when creating requests.

Rules:
- Admin may create a request for any course.
- Lecturer may create a request only for courses they teach or have lecturer membership for.
- Student may create a request only for courses they are enrolled in.
- Any authenticated user may create a global request if `courseId` is null.
- If `courseId` is invalid or the user lacks access, return a clean 403 or 404 error.

Add tests for:
- student can create request for enrolled course
- student cannot create request for non-enrolled course
- lecturer can create request for taught course
- lecturer cannot create request for unrelated course
- admin can create request for any course
- global request without course is allowed

Likely files:
- `artifacts/api-server/src/services/requests.service.ts`
- `artifacts/api-server/src/repositories/requests.repo.ts`
- request service tests

4. Remove stale Drizzle/localStorage comments

Clean stale comments/docs that imply:
- Drizzle is still used
- restricted docs are visible to all authenticated users
- recently viewed is localStorage-only

The current system should be described as:
- Prisma + PostgreSQL
- course-aware permissions
- backend-powered recently viewed with localStorage fallback only

5. Final verification

Run and report results:

pnpm --filter @workspace/db run generate
pnpm --filter @workspace/db run migrate
pnpm run typecheck
pnpm --filter @workspace/api-server test
pnpm --filter @workspace/api-server seed:demo
pnpm --filter @workspace/api-server seed:demo:verify

Acceptance criteria:
- Rich demo data is loaded by the expected demo/startup flow.
- Demo login buttons work with the new demo accounts.
- README/Replit docs show the correct credentials and Prisma stack.
- Request creation is course-aware.
- No Drizzle runtime code is reintroduced.
- No Python/FastAPI is added.
- Typecheck passes.
- Backend tests pass.
- Demo seed verification passes.

After implementation, provide:
1. Files changed.
2. Commands run.
3. Test results.
4. Updated demo credentials.
5. Any remaining limitations.