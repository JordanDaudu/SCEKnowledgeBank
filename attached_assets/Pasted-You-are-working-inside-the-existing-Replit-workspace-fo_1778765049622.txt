You are working inside the existing Replit workspace for the Group 1 Knowledge Bank project.

Do NOT restart the project.
Do NOT replace the architecture.
Do NOT switch to Prisma.
Do NOT create a new unrelated client/server structure.

Use the Sprint 1 foundation as the source of truth.

==================================================
CURRENT PROJECT STATE FROM SPRINT 1
==================================================

The project is a standalone modular monolith Knowledge Bank / בנק ידע application.

The approved architecture is:

- React + Vite + TypeScript frontend
- Express + TypeScript backend
- PostgreSQL database
- Drizzle ORM, not Prisma
- OpenAPI as the API contract
- Orval-generated React/TanStack Query client
- Zod validation
- Local file storage abstraction now, cloud-ready later
- Docker-ready structure
- Demo/seed data

Important workspace mapping:

- Backend lives in:
  artifacts/api-server/

- Frontend SPA should live in:
  artifacts/web/

- Shared API contract lives in:
  lib/api-spec/openapi.yaml

- Generated React API client lives in:
  lib/api-client-react/

- Generated Zod schemas live in:
  lib/api-zod/

- Database layer lives in:
  lib/db/

- File storage abstraction should live in:
  lib/storage/

Use this workspace-native structure.

==================================================
SPRINT 2 GOAL
==================================================

Implement the standalone Sprint 2 MVP baseline.

The system must be demoable end-to-end:

upload → browse → search/filter → preview/download → comment → request missing material → enforce basic RBAC.

The app must work as a standalone system today while keeping extension points for later features.

==================================================
MANDATORY IMPLEMENTATION AREAS
==================================================

1. Database / Drizzle

Create or update Drizzle schema files in lib/db/src/schema/ for:

- users
- roles
- userRoles
- permissions / rolePermissions if practical
- courses
- categories
- tags
- documents
- documentFiles
- documentTags
- comments
- materialRequests
- requestVotes
- materialViewHistory
- auditLogs

Use createdAt and updatedAt on important tables.
Use deletedAt where soft delete is needed.

Document and file metadata must be separated:
- Document = conceptual academic material
- DocumentFile = actual uploaded file metadata

Add indexes needed for:
- course filtering
- lecturer filtering
- material type filtering
- semester/year filtering
- date filtering
- comment lookup
- request lookup
- audit lookup

2. OpenAPI contract

Update lib/api-spec/openapi.yaml first.

Define endpoints for:

Auth:
- POST /auth/login
- POST /auth/logout
- GET /auth/me

Documents:
- GET /documents
- POST /documents/upload
- POST /documents/bulk-upload if practical, or support multiple files in upload endpoint
- GET /documents/{id}
- PATCH /documents/{id}
- DELETE /documents/{id}
- GET /documents/{id}/preview-token
- GET /documents/{id}/download-token
- GET /documents/{id}/preview
- GET /documents/{id}/download
- GET /documents/suggestions
- GET /documents/recent

Comments:
- GET /documents/{id}/comments
- POST /documents/{id}/comments
- DELETE /comments/{commentId}

Requests:
- GET /requests
- POST /requests
- POST /requests/{id}/vote
- PATCH /requests/{id}

Taxonomy:
- GET /courses
- GET /categories
- GET /tags

Admin/demo if needed:
- GET /users

After updating OpenAPI, make sure generated clients/schemas can be regenerated through the existing Orval setup.

3. Backend

Implement in artifacts/api-server/src/.

Follow this structure:

- routes
- controllers
- services
- repositories
- middlewares
- config
- lib utilities

Controllers must stay thin.
Business logic goes in services.
Drizzle calls go in repositories.

Implement:

Auth:
- demo login
- session or simple demo auth mechanism
- current user endpoint
- seeded users:
  - student@demo
  - lecturer@demo
  - admin@demo

RBAC:
- requireAuth middleware
- requireRole middleware
- course/action permission hook
- backend enforcement, not only frontend hiding

Documents:
- paginated document list
- search query q
- filters:
  - courseCode
  - lecturerName
  - semester
  - academicYear
  - materialType
  - categoryId
  - tagIds
  - dateFrom
  - dateTo
- sorting:
  - newest
  - oldest
  - title
  - popularity
- upload single file
- upload multiple files baseline
- file size validation
- MIME validation
- safe stored filenames
- checksum or duplicate detection
- duplicate name handling strategy
- metadata extraction from filename where practical
- metadata edit
- soft delete
- preview/download flow

Storage:
- create lib/storage if not already present
- define StorageAdapter interface
- implement local adapter
- keep S3/GCS as future stub only
- do not put storage logic directly in routes

Preview/download security:
- implement signed/timed token utility
- preview/download endpoints must validate signed token
- frontend should request token before opening preview/download

Recently viewed:
- record view when document detail or preview is opened
- expose recent documents endpoint

Comments:
- comments linked to document and user
- nested replies using parentId
- pageNumber support for PDF comments
- mention-ready parsing or storage hook

Requests:
- create missing-material request
- list requests
- filter by course/status if practical
- vote once per user
- enforce duplicate vote prevention in backend and DB

Audit:
- create audit service
- record important mutations:
  - document uploaded
  - document updated
  - document deleted
  - comment added
  - request created
  - request voted

4. Frontend

Create or update artifacts/web/.

Use:

- React
- TypeScript
- Vite
- React Router
- TanStack Query
- generated API client from lib/api-client-react when possible

Implement pages:

- LoginPage
- DashboardPage
- DocumentsListPage
- DocumentUploadPage
- DocumentDetailPage
- RequestsBoardPage
- Admin/demo page if practical

Implement reusable components:

- AppShell
- Sidebar
- TopBar
- Button/Input/Select/Badge/Card/Modal basics
- LoadingState
- ErrorState
- EmptyState
- NoResultsState
- FileDropZone
- UploadQueue
- DocumentFilters
- DocumentTable or DocumentCards
- PreviewPanel
- CommentsThread
- RequestBoard

Frontend flows:

Documents list:
- search input
- filters
- sorting
- pagination
- no results state
- loading/error states

Upload:
- drag-and-drop area
- visual drag hover state
- single and multiple file selection
- per-file queue status
- progress display if practical
- validation errors visible to user
- metadata prefill/edit before or after upload

Detail:
- metadata panel
- preview panel
- download button
- edit metadata modal
- comments section
- page-linked comment input for PDFs

Requests:
- create request
- list requests
- vote once
- show vote count

Auth:
- login as demo user
- route protection
- role-aware UI
- backend remains source of truth

==================================================
SPRINT 2 USER STORY COVERAGE
==================================================

Implement coverage for:

File Engine:
- US-1 drag-and-drop upload
- US-2 bulk upload queue
- US-3 advanced validation
- US-4 browser preview
- US-5 versioning-ready foundation
- US-6 metadata extraction/prefill
- US-7 progress tracking baseline
- US-8 duplicate handling
- US-9 thumbnails/fallback icons
- US-10 storage quota-ready foundation

Smart Discovery:
- US-11 search
- US-12 lecturer filter
- US-13 course code filter
- US-14 semester/year filter
- US-15 material type filter
- US-16 upload date range filter
- US-17 sort by popularity
- US-18 search suggestions
- US-21 recently viewed
- US-22 pagination

Collaboration:
- US-23 nested comments
- US-24 mention-ready comments
- US-25 page-linked PDF comments

Request Board:
- US-57 request missing material
- US-58 requested materials board
- US-59 vote for request

Security:
- US-33 RBAC
- US-34 granular course/action permission foundation
- US-38 signed/timed preview and download access

==================================================
SEED DATA
==================================================

Create realistic seed data:

- roles
- users
- courses
- categories
- tags
- documents
- documentFiles metadata
- comments
- material requests
- request votes
- recent views
- permissions

Seed enough data to demo filters and search:
- at least 6 courses
- multiple lecturers
- multiple semesters
- multiple academic years
- several material types
- comments on multiple documents
- several open requests

==================================================
CONFIG / DOCKER / DOCS
==================================================

Create or update:

- .env.example
- docker-compose.yml
- Dockerfile(s) where appropriate
- README.md or replit.md

Document:
- setup
- env vars
- seed command
- run command
- demo users
- main flows
- known TODO/future hooks

Required env vars should include:
- DATABASE_URL
- SESSION_SECRET or JWT_SECRET
- PORT
- WEB_ORIGIN
- STORAGE_DRIVER
- STORAGE_LOCAL_ROOT
- MAX_UPLOAD_MB
- ALLOWED_MIME_TYPES
- SIGNED_URL_SECRET
- SIGNED_URL_TTL_SECONDS

==================================================
QUALITY RULES
==================================================

Do not only scaffold.
Build working flows.

Do not leave fake buttons.
If something is not fully implemented, either:
- make it work simply, or
- clearly mark it as a future hook and keep the UI honest

No hardcoded secrets.
No DB logic in controllers.
No storage logic in routes.
No permission logic only in frontend.
No giant React components.
No giant backend route files.

==================================================
EXECUTION ORDER
==================================================

1. Inspect the existing workspace.
2. Preserve existing architecture and package manager.
3. Update Drizzle schema and seed data.
4. Update OpenAPI spec.
5. Implement backend modules.
6. Implement frontend app.
7. Wire frontend to generated/client API.
8. Add Docker/config/docs.
9. Run typecheck/build where possible.
10. Report:
   - files changed
   - features implemented
   - commands to run
   - remaining TODOs
   - known limitations

Start now by inspecting the existing files and continuing from the Sprint 1 foundation.