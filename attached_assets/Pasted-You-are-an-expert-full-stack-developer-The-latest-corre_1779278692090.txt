You are an expert full-stack developer. The latest corrected Knowledge Bank project mostly implements the Sprint 2 follow-up successfully. Do not rewrite the project. Keep the existing React/Node/Express/Prisma/PostgreSQL architecture. Implement only the final hardening fixes below.

1. Block restricted uploads without a course

In the backend upload service, before storing files or creating documents, reject any upload where:

visibility === "restricted" && !courseId

Return a clean 400 validation error:

"Restricted documents must be linked to a course."

Also ensure restricted documents cannot be created with an invalid/nonexistent courseId.

Add tests for:
- restricted upload without courseId is rejected
- restricted upload with valid enrolled/taught course succeeds
- lecturer cannot upload restricted material to a course they do not teach
- invalid courseId returns a clean validation error

Likely files:
- artifacts/api-server/src/services/documents.service.ts
- artifacts/api-server/src/routes/documents.ts
- artifacts/api-server/src/tests/documents.upload.test.ts

2. Strengthen upload body validation

The multipart upload route currently parses fields manually. Add proper validation before calling the service.

Validate:
- visibility is one of: public, restricted, private
- materialType is one of the existing supported material types
- courseId is a valid UUID if provided
- categoryId is a valid UUID if provided
- tagIds are valid UUIDs if provided
- semester/academicYear values follow the project’s existing conventions

Return clean 400 errors instead of generic upload_failed errors when request metadata is invalid.

3. Add requested admin route aliases

Keep the existing admin-protected user routes, but also add aliases:

GET /api/admin/users/pending-lecturers
POST /api/admin/users/:userId/approve
POST /api/admin/users/:userId/disable

These should call the same logic as the current routes:

GET /api/users/pending-lecturers
POST /api/users/:id/approve
POST /api/users/:id/disable

Update OpenAPI and generated clients if the project expects them.

4. Strengthen demo seed verification for full-text search

Update seed-demo.verify.ts so it proves full-text extracted-content search actually works.

Do not only check that extractedText exists.

Add a verification that performs a real document search using the same repository/service/API used by the app and confirms that a seeded document is returned because of a term inside extractedText.

Example:
- Search for a seeded term like "sprint retrospective", "metadata extraction", or another unique phrase that exists in extractedText.
- Confirm the expected seeded document is returned.

5. Make demo metadata backfill type-aware

Clean up the demo seed metadata backfill logic so it does not repeatedly re-extract files just because unsupported metadata fields are null.

Rules:
- PDF: expect pageCount and extractedText where supported
- text/markdown/csv: expect extractedText
- image: expect imageWidth/imageHeight and thumbnail metadata
- Office files: expect fallbackIconType; deep text extraction is optional
- unknown files: fallbackIconType is enough

6. Clean stale comments

Clean any stale comments suggesting recently viewed is localStorage-only. Backend history is now the source of truth; localStorage is only fallback.

7. Final verification

Run:

pnpm --filter @workspace/db run generate
pnpm --filter @workspace/db run migrate
pnpm run typecheck
pnpm --filter @workspace/api-server test
pnpm --filter @workspace/api-server seed:demo
pnpm --filter @workspace/api-server seed:demo:verify

Acceptance criteria:
- Restricted uploads cannot be created without a course.
- Upload metadata validation returns clean errors.
- Admin route aliases work.
- Seed verification proves full-text search over extracted content.
- Typecheck passes.
- Backend tests pass.
- Demo seed and verification pass.
- No Drizzle runtime code is reintroduced.
- No Python/FastAPI is added.