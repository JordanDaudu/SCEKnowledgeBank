You are continuing the existing Knowledge Bank / בנק ידע project.

Important:
Do NOT restart the project.
Do NOT replace the current architecture.
Do NOT remove existing implemented features.
Work inside the existing codebase and finish Sprint 2 acceptance quality.

Context:
The project already has a broad Sprint 2 implementation:
- API server
- web app
- DB schema
- auth/session
- RBAC
- upload flow
- browse/search/filter/sort
- document detail
- signed preview/download
- comments
- requests board
- audit logs
- seed data
- Docker setup
- generated API/Zod packages

Your goal now is to harden and complete Sprint 2, fixing missing acceptance items and demo-breaking issues.

Before coding:
1. Read the Sprint 2 task file:
   /.local/tasks/knowledge-bank-sprint-2-mvp.md

2. Inspect the current implementation, especially:
   - artifacts/web/src/pages/upload.tsx
   - artifacts/web/src/pages/browse.tsx
   - artifacts/web/src/pages/document-detail.tsx
   - artifacts/web/src/pages/requests.tsx
   - artifacts/api-server/src/services/documents.service.ts
   - artifacts/api-server/src/services/requests.service.ts
   - artifacts/api-server/src/routes/documents.routes.ts
   - artifacts/api-server/src/lib/env.ts
   - artifacts/api-server/src/index.ts
   - lib/db/src/schema/*
   - lib/api-spec/openapi.yaml
   - docker-compose.yml
   - .env.example
   - replit.md

Main fixes required:

1. Fix API base URL handling for frontend upload, preview, and download.
   Problem:
   Some frontend code uses relative URLs like /api/documents/upload or signed URLs returned as /api/documents/:id/preview?token=...
   This can break when the web app and API run on different origins, such as frontend on localhost:5173 and API on localhost:8080.

   Required:
   - Create one central frontend helper for resolving API URLs using VITE_API_BASE.
   - Use it for upload XHR, preview iframe URLs, and download window.open URLs.
   - Keep upload progress support.
   - Do not leave hardcoded /api paths in page components.
   - Alternatively, if you choose backend absolute signed URLs, add API_PUBLIC_ORIGIN to env validation, .env.example, docker-compose, and docs.

2. Fix upload endpoint usage.
   - Avoid raw duplicated endpoint strings in upload.tsx.
   - If XHR is still required for progress, use a generated URL helper or central URL builder.
   - Keep progress bar behavior.

3. Fix duplicate filename behavior.
   Sprint 2 requires duplicate names to be displayed with suffixes like filename (2).pdf while preserving the original uploaded filename.
   Current behavior appears to overwrite documentFiles.originalFilename with the suffixed name.

   Required:
   - Preserve document_files.originalFilename as the exact user-uploaded filename.
   - Add a separate field for the resolved/display filename, for example displayFilename or resolvedFilename.
   - Update DB schema, API types, OpenAPI schema, seed if needed, generated clients, upload response, and frontend display.
   - Show the duplicate rename clearly in the upload result UI.

4. Align frontend and backend file type validation.
   - Make the upload page allowed file types exactly match backend MIME/content-sniffing support.
   - Either remove gif/webp from frontend if backend does not support them, or properly add backend support.
   - Make frontend max upload size match backend configuration instead of using an unrelated hardcoded value.

5. Fix Requests Board RBAC mismatch.
   Problem:
   Frontend shows Fulfill action for lecturer/admin, but backend appears to allow request status update only for owner/admin.
   Required:
   - Decide the product rule for Sprint 2:
     lecturer/admin should be able to fulfill requests.
   - Update backend authorization accordingly.
   - Update frontend visibility accordingly.
   - Add/adjust service tests for:
     student cannot fulfill someone else's request
     lecturer can fulfill
     admin can fulfill
     duplicate vote still blocked

6. Complete Browse page Sprint 2 UI requirements.
   Required:
   - Add card/table toggle.
   - Extract DocumentCards component.
   - Extract DocumentTable component.
   - Add search suggestions while typing using the existing /documents/suggestions endpoint.
   - Add a Recently Viewed strip to the Browse/Documents page.
   - Add clear loading, empty, no-results, and error states.

7. Split oversized frontend page files.
   Current files are too large and mix responsibilities.
   Refactor into feature components without changing behavior:
   - browse filters
   - browse cards
   - browse table
   - recently viewed strip
   - document preview
   - metadata panel
   - edit metadata modal
   - comments thread
   - upload dropzone
   - upload queue
   - upload metadata form

   Keep imports clean and avoid circular dependencies.

8. Add proper DB migration support for Sprint 2.
   - Generate Drizzle migration files for the current schema.
   - Ensure pg_trgm extension is created through migration or clearly handled in DB init.
   - Do not rely only on undocumented schema push.
   - Keep Docker demo working.

9. Add seed fixture files.
   Sprint 2 requested sample PDFs/images in:
   lib/db/src/seed/fixtures/

   Required:
   - Add real fixture files or simple generated fixture assets committed in that folder.
   - Update seed script to use fixtures for at least several documents.
   - Keep the current rich seed data volume: demo users, courses, categories, tags, 30+ documents, comments, and requests.

10. Improve env validation.
   - Replace or wrap current env validation with Zod-based validation.
   - Ensure PORT has a safe default or is consistently required and documented.
   - Add any new required env vars to .env.example, docker-compose.yml, and replit.md.

11. Verify OpenAPI and generated clients.
   - Update lib/api-spec/openapi.yaml for any schema changes.
   - Regenerate api-zod and api-client-react.
   - Make sure frontend imports generated types/hooks where appropriate.
   - Avoid hand-written API response types when generated types exist.

12. Run final checks and fix all failures:
   - pnpm install if needed
   - pnpm --filter @workspace/api-spec run codegen
   - pnpm run typecheck
   - pnpm run build
   - pnpm test if available
   - docker compose up --build

13. Update replit.md and README if anything changed.
   Include:
   - demo login users
   - required env vars
   - upload limits
   - how signed preview/download URLs work
   - how to run locally
   - how to run Docker demo
   - known limitations, if any

Acceptance checklist:
- student@demo / lecturer@demo / admin@demo can log in with demo1234.
- Lecturer/admin can upload multiple files.
- Unsupported file types show immediate client validation and server validation.
- Oversized files show a clear error.
- Duplicate filenames are suffixed for display but original filename is preserved.
- Browse page supports search, suggestions, filters, sorting, pagination, card/table view, recently viewed, loading, empty, no-results, and error states.
- Document detail preview works for PDFs/images.
- Download works from the correct API origin.
- Direct preview/download without token is rejected.
- Expired/invalid token is rejected.
- Comments support nested replies, page pin, and @mention text.
- Requests board supports create, vote once, status display, and lecturer/admin fulfillment.
- RBAC is enforced server-side, not only in the UI.
- Audit logs are written for upload/update/delete/comment/request/vote.
- Typecheck passes.
- Build passes.
- Docker demo works from a clean start.